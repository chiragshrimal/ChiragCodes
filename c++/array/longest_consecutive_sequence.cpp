#include<bits/stdc++.h>
using namespace std;
// brute force apporach 
// time complexity is O(n^2)
// space complexity is O(1);

int main(){
    int n;
    cin>>n;
    vector<int> v(n,0);
    for(int i=0;i<n;i++){
        cin>>v[i];
    }
}